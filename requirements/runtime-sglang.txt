# Direct runtime distributions from the vendored SGLang 0.4.6.post1
# pyproject.  The vendored fork itself is installed separately with --no-deps.
aiohttp
cachetools==5.5.2
openai==1.109.1
tiktoken==0.13.0
requests
tqdm
setproctitle
ipython
compressed-tensors
# decord is intentionally excluded from the Python 3.11 text-only runtime; install it only for optional video/multimodal workloads.
fastapi
hf-transfer
huggingface-hub
interegular
llguidance>=0.7.11,<0.8.0
ninja
orjson
pillow
prometheus-client>=0.20.0
psutil
pydantic
pynvml
python-multipart
pyzmq>=25.1.2
soundfile==0.13.1
torchao==0.9.0
uvicorn
uvloop
xgrammar==0.1.17
outlines>=0.0.44,<=0.1.11
partial-json-parser
einops
cuda-python
flashinfer-python==0.2.5
modelscope

# These native/runtime wheels are pinned to the exact dual-T4 stack that passed
# the real Kaggle pre-backward probe. The installer handles them in a guarded
# binary-only step before installing the vendored forks with --no-deps.
sgl-kernel==0.1.1
torch-memory-saver==0.0.8

# flash-attn 2.7.3 wheel/ABI remains target-only and is not resolved on macOS.
