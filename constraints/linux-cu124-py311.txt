# CANDIDATE ONLY -- target_machine_test_deferred
# Target: CPython 3.11.x / Linux / official PyTorch CUDA 12.4 wheel family.
# This is not a verified lock and must not be labelled requirements_lock_verified.
torch==2.6.0
torchvision==0.21.0
transformers==4.51.1
flashinfer-python==0.2.5
flash-attn==2.7.3

# Deliberately not pinned here pending target probes:
# ray, numpy, pyarrow, tensordict and torchdata. sgl-kernel is fixed at 0.1.1
# by the active runtime requirements and target installer.
# System driver, CUDA toolkit, NCCL, cuDNN, GCC and glibc are not Python
# requirements and must never be added to this file.
accelerate==1.10.1
datasets==4.1.1
dill==0.4.0
fsspec==2025.9.0
multiprocess==0.70.16
peft==0.17.1
compressed-tensors==0.11.0
frozendict==2.4.6
torchao==0.9.0
